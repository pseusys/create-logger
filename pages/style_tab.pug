.tile-set-left
    .tile
        +colors('for', "Front color")
        +colors('back', "Back color")
    .tile
        each style in styles
            .gr
                input.term-changer(id=style value=style name="sty" type="radio")
                label.ins(for=style)=style
    .tile
        +simple('blink', 'cross', 'under', 'ita')

.tile-set-center
    +presets({ error: "back-red sty-bold", warning: "for-yellow", info: "for-blue", plain: "" })
    //TODO: replace following with values read from cookies.
    +presets({ modern: "", beautiful: "", original: "", creative: "" })

.tile-set-right
    .tile
        .gr-in
            .gr
                input#is-variable(type="checkbox")
                label.ins(for="is-variable") Is this a variable?
            .gr-in
                input#var-name(type="text" placeholder="Variable name")
            .gr-in
                input#var-type(type="text" list="types-list" placeholder="Variable type")
                datalist#types-list
                    each type in types
                        option(value=type)
